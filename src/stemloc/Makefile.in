# Requires GNU Make

TESTS      = 
TARGETS    = $(TESTS) stemloc tetris gc2gr-ss savedefaults refloc
DIRNAME    = stemloc

DEPS       = amap telegraph scfg hmm seq util newmat randlib

CCDIR      = $(shell pwd)
SRCDIR     = $(CCDIR)/..

include $(SRCDIR)/make.common
