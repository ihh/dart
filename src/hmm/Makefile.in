# Requires GNU Make

TESTS      = testhmm
TARGETS    = $(TESTS) gibbshmm
DIRNAME    = hmm

DEPS       = seq util newmat randlib

CCDIR      = $(shell pwd)
SRCDIR     = $(CCDIR)/..

include $(SRCDIR)/make.common
