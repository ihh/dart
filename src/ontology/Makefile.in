# Requires GNU Make

TESTS      = 
TARGETS    = $(TESTS) 
DIRNAME    = ontology

DEPS       = ecfg @GUILE_DEPS@ scfg irrev hsm hmm tree seq util newmat randlib

CCDIR      = $(shell pwd)
SRCDIR     = $(CCDIR)/..

include $(SRCDIR)/make.common
