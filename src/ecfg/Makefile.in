# Requires GNU Make

TESTS      = 
TARGETS    = $(TESTS) xrate xfold xprot xgram simgram paircfg_xfold testsexpr
DIRNAME    = ecfg

DEPS       = $(GUILE_DEPS) amap telegraph scfg hmm hsm irrev tree seq util newmat randlib

CCDIR      = $(shell pwd)
SRCDIR     = $(CCDIR)/..

include $(SRCDIR)/make.common
