# Requires GNU Make

# TESTS and TARGETS should only contain files with a main() function
TESTS      = 
TARGETS    = $(TESTS)
DIRNAME    = amap

DEPS       = seq util

CCDIR      = $(shell pwd)
SRCDIR     = $(CCDIR)/..

include $(SRCDIR)/make.common
